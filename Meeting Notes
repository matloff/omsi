General
	Router gives secret IP addresses and public IP addresses
	Prof. starts web server in office with static IP address
	Wireshark would see secret addresses, not public IP addresses

At home, run server on csif

what determines whether a machine is given a static IP address?
	32 bit addresses initially, pre-allocated
	Router has static IP address
	Router gives temporary IP addresses
	



vishal
	abstract
monte

